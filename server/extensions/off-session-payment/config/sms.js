const PROVIDER_MESSAGE =
  "The customer's payment for one of the the booking requests could not be captured successfully, so the request cannot be processed. Please wait for the customer to retry the payment before the auto-expire action occurs";
const CUSTOMER_MESSAGE =
  'The payment for one of your booking requests failed, so it cannot be processed. Please retry before it expires.';

module.exports = {
  PROVIDER_MESSAGE,
  CUSTOMER_MESSAGE,
};
